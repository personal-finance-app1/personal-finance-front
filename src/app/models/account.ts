export class Account {
    
    constructor(public income: number, public expenses: number, public balance: number){
        this.income = income;
        this.expenses = expenses;
        this.balance = balance;
    }
}
